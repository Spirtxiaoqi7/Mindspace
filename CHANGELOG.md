# 更新历史

Mindspace 使用语义化版本号。机器可读的同源记录位于
[`docs/release-history.json`](docs/release-history.json)。

## 0.7.0 - 候选版本

### 共同篇章与二次元典藏手账

- 新增按角色隔离的日记、共同片段与活动会话仓库，不再把玩法字段塞入 `runtime_state`。
- 新增场景同行、默契问答和片刻故事；所有阶段变化走服务端 revision 与幂等 action，
  模型只负责角色表达。
- 日记仅在用户点击时最多调用一次模型，失败使用本地草稿；日记与片段固定为
  `narrative_only`，不能作为人物档案写回证据。
- 普通聊天模型调用数不增加；已保存叙事只在当前角色的相关检索命中时进入 Prompt。
- 聊天顶部和输入区增加共同篇章入口，提供日记编辑、候选片段确认、活动中断与恢复。
- 12 项原创二次元手账预览通过视觉审核，扩产为 171 项内置资源：72 个紧裁剪图标、
  24 个贴纸、16 套边框、12 套纹理、12 张状态图、20 张场景图、8 套封面及 7 项兼容资源。
- 页面已从临时 `previews` 路径切换到正式资源路径；场景同行开放 20 张场景卡，日记支持
  8 套封面，输入区、共同片段和空状态换用正式图标与插画。
- 增加可重复执行的 `scripts/build_art_library.py`，统一安全裁剪、WebP 压缩、Manifest
  字节数与 SHA-256，并硬校验内置资源不超过 15MB。
- 增加可选资源包的断点续传、SHA-256、ZIP 安全校验、staging 与失败回滚基础。
- Core / Web 升至 0.7.0；Launcher 保持 0.5.52。功能 Beta 闭环完成前不发布热更新。

## 0.6.0 - 2026-07-30

### 多角色架构与灵感抽卡

- Core 就绪后首先显示模式大厅，提供“灵感抽卡”和原有完整自定义工作台。
- 新增角色权威记录，AI 档案、头像、关系、运行状态、共同经历、会话与长期召回均按
  `character_id` 隔离；用户基础档案与手动知识库继续全局共享。
- 新建会话必须先选择角色，已有消息的会话禁止静默换绑；Prompt Inspector 显示当前角色
  和各上下文层的作用域。
- 灵感抽卡以名字、性别、两个核心性格、一个人格缺陷、关系与称呼为输入，每次最多调用
  一次 LLM；30 秒超时、API 未配置或返回无效 JSON 时使用确定性本地模板。
- 新增典藏卡册、角色预览与高级 JSON 编辑、复制、归档、版本恢复，以及
  `.mindspace-card` 安全导入导出。
- 首次启动事务化创建“原有角色”，绑定旧会话，并在迁移前备份档案、头像和会话投影；
  数据库失败时不写完成标记或半迁移角色。
- 新增原创二次元占位头像、模式卡框、性格图标、关系印章、操作图标、响应式底纹与减少
  动效降级；全部新增美术资源小于 1MB。
- Core / Web 升至 0.6.0；Launcher 继续保持 0.5.52，并通过现有签名更新协议安装 Core。

## 0.5.52 - 2026-07-29

### Python 完整性强校验与自动修复

- Python 就绪条件从仅执行 `--version` 升级为标准库文件检查，并实际导入 `encodings`、
  `ensurepip` 和 `venv`，防止残缺运行时被错误标记为可用。
- 旧版 Python 标记会自动重新验证；完整环境直接复用，不完整环境自动调用 uv
  `python install --reinstall` 原位修复，不要求用户手工删除目录。
- 修复只处理当前 Python 运行时，保留 PowerShell、Git、uv、Core 源码、下载断点、模型、
  人物卡、RAG 和用户数据。
- `encodings` 错误不再武断显示为“路径污染”，改为“运行时不完整或与旧环境冲突”，并明确
  提示用户再次点击“一键安装”即可继续。
- 运行时日志新增 `python.revalidated`、`python.invalid` 和 `python.incomplete`，便于区分
  完整复用、导入失败和标准库文件缺失。

## 0.5.51 - 2026-07-29

### 私有 Python 启动环境隔离

- 修复 Launcher 继承用户系统 `PYTHONHOME`、`PYTHONPATH`、Conda、venv 或 uv 项目环境后，
  私有 Python 错把旧 `application\core` 当成标准库根目录并报
  `ModuleNotFoundError: encodings` 的问题。
- 所有私有 Python、uv、Core 和组件安装子进程启动前，大小写无关地清除宿主 Python 与
  虚拟环境状态，再显式注入 Mindspace 私有运行时路径。
- Python 路径污染现在使用稳定错误码和简短恢复提示，不再把完整 `sys.path`、线程地址与
  Fatal Python 堆栈撑满首次引导页面。
- 失败安装留下的 Python 下载和已完成组件可以直接复用；用户再次点击“一键安装”即可从
  Core 环境阶段继续，不要求删除配置或重新下载基础工具。

## 0.5.50 - 2026-07-29

### 低带宽下载控制面与安装回退

- 修复首次引导下载域名无法解析时直接失败的问题：国内优先入口失败后会自动切换官方上游，
  Python 与 PyPI 同样采用一次受限回退；界面显示实际下载域名、回退状态和基础环境总进度，
  不再把 Electron IPC 原始错误或聚合进度误显示为 PowerShell 下载状态。
- 运行时清单改用已部署的 `douyinqijun.cn/downloads/mindspace` 控制入口并重新执行 Ed25519
  签名；清单仍保留各组件的权威官方 URL 作为有限回退。
- 官网运行时入口仅返回轻量 `302`，不在低带宽服务器保存或中转 PowerShell、MinGit、uv、
  Python、ASR、TTS 与模型大包。
- 国内入口、显式多来源清单和官方源使用同一套有限回退策略，不在来源之间循环；下载断点、
  SHA-256 校验和私有运行时隔离保持不变。

## 0.5.49 - 2026-07-29

### NSFW 成年确认与语音状态同步

- 成人模式入口精简为 `NSFW`；首次开启必须等待三秒并确认本人已成年，确认结果仅保存在
  本机，后续开启或关闭不再重复弹窗。
- 启动器与应用内 TTS 使用同一配置源，GPT-SoVITS、CosyVoice、Qwen3、SiliconFlow 和
  关闭声音均能正确显示与互斥切换。
- 修复应用内 TTS 热同步导致设置窗口重建，以及配置文件临时不可读时误切换声音的问题。
- 新手引导支持返回上一步并保留状态；修复 140% 缩放下返回按钮遮挡和维护图标断行。
- 安装器与用户工作区彻底分离：静默卸载和普通卸载都不再删除环境、模型、会话、人物卡、
  RAG、参考音频或下载断点；数据清理只能由应用内显示真实路径后单独执行。
- TTS 输出按当前 Provider 路由：只有 Qwen3 接收语气事件和整轮合成协议，GPT-SoVITS、
  CosyVoice 及其他流式引擎不再收到无效 Qwen 标签；响应头公开 Provider 与读取模式。
- R18 语音长度、推进结构和质量门控进入同一次正文生成；私有写法素材按轮抽取但不暴露
  内部存储字段，不增加第二次模型调用。

## 0.5.48 - 2026-07-29

### 首次引导与三引擎语音切换

- 首次配置按声音、基础环境、对话模型和准备完成四步引导；支持返回此前步骤，已经选择的
  声音、断点下载进度与模型配置不会被清空。
- 启动器增加 GPT-SoVITS、CosyVoice、Qwen3-TTS 和关闭声音的常驻切换入口；应用内修改
  TTS 后，启动器会读取同一配置并同步服务状态。
- TTS 切换采用互斥调度：先停止旧 Worker 并释放端口或显存，再启动新 Worker；GPT 与
  Cosy 共用 5055 时也会执行完整重启，Qwen 不会与另一套本地 TTS 双开。
- 未安装的新声音会立即成为明确的待用选择，并进入可取消、可续传的后台安装队列；文字
  对话和 Core 不等待语音组件。

## 0.5.47 - 2026-07-29

### R18 单次生成强度阶梯

- 通用 R18 Director 增加六档递进：明确请求、主动勾引、直白表达、淫语与实质行为、
  高强度喘叫、赞赏与高潮反馈；R18 开启后要求角色持续向性行为方向推进。
- 最近六轮的“你先说、我再考虑、待会、让我想想、怎么来”等拖延话术，以及已记录的
  `r18_missing_sexual_action`，会直接提高下一轮最低档位，换同义句不能重置进度。
- 语音的“只说对白”不再被解释为“不能发生行为”：角色必须用实时反应、直接命令、
  淫语、喘息和身体反馈证明行为已经发生，同时继续禁止括号和动作旁白。
- 强度阶梯直接进入唯一一次正文生成；质量诊断不触发第二轮模型重写，普通成人轮仍只
  使用一次 generation 调用。

## 0.5.46 - 2026-07-29

### Qwen 整轮单次合成与角色去理想化

- Qwen 实时语音不再抢跑首句：模型正文完成格式清理后，通过新的 `response.ready` 事件把
  完整回复一次提交给 CustomVoice；一句回复只建立一个声学上下文，避免前后段声线变化、
  韵律断裂和重复排队。
- `response.ready` 位于格式归一化和能力声明保护之后、角色审计与持久化之前，因此不需要
  等待数据库收尾才开始合成；`run.completed` 仅在前一事件丢失时兜底，不能重复朗读。
- 角色演绎明确拒绝理想化伴侣与高素质客服模板：人物卡中的嫉妒、占有欲、控制欲、偏见、
  嘴硬、自私和坏习惯会实际影响选择；日常说话允许适量吐槽、挖苦、粗口和不耐烦。
- R18 模式允许符合角色口吻的直白淫语、器官及性行为俗称和脏话，不再自动改写成体面、
  医学化或只有暧昧暗示的表达；语音仍只输出角色亲口说出的内容，不朗读动作旁白。

## 0.5.45 - 2026-07-29

### 纯口语面对面与括号静音

- 实时语音和面对面模式只输出角色亲口说出的自然口语，不再把动作、神态或体感改写成
  “我正在……”一类第一人称动作播报。
- 前端流式分段器与 Core 双层丢弃圆括号及其中内容；旧消息、模型偶发格式错误和跨 token
  括号都不能再把舞台说明送入 TTS。
- 普通语音回复通常形成三至五句、约七十至一百五十个中文字符的完整回合，减少过短回答
  造成的频繁来回请求，但禁止用重复套话凑长度。
- R18 语音允许直接口述角色的舒服、欲望、喘息、反馈、露骨评价和低俗成人台词，同时继续
  禁止动作说明书、镜头旁白和括号朗读。

## 0.5.44 - 2026-07-29

### Qwen 首句抢跑与延迟闸门清理

- 移除 Qwen 必须等待整轮 LLM 回复完成才提交 TTS 的前端闸门。首个完整口语句在流式到达时立即
  进入合成，余下正文合并为一个大段串行处理，不恢复逐句并发请求。
- 保留 CustomVoice、`Serena`、固定 seed、同一语气指令与单合成队列；一次回复通常只形成
  “首句 + 余下大段”两个声学请求，在降低首声等待的同时限制跨段声线漂移。
- “嗯……”“呵……”等开场犹豫或轻笑必须和第一个真实句子一起合成，不能单独占用请求和播放队列。
- 本机日志显示 Qwen 热态首个 PCM 通常约 `125–165 ms`；此前常见的额外 `3.5–5.1 s`
  来自等待正文生成完毕，而非音频流首包。

## 0.5.43 - 2026-07-28

### 恢复 CustomVoice 与声线锁定

- Qwen3 恢复为 `1.7B-CustomVoice` 和默认 `Serena`。Base 克隆虽然稳定声纹，但不支持
  `instructions`，会把笑声、呼吸、语速与情绪压成平直念稿。
- 声线一致性改由四层确定性约束保证：固定模型权重、固定 `Serena` speaker、固定 seed、
  整轮仅一次合成。风格指令只描述韵律，不再描述年龄、音高或音色，避免重新塑造 speaker。
- 普通回复根据正文中的“呵／哈”“呼／唔”“唉／哎”自动选择一次轻笑、换气或叹息演绎；
  显式语气仍优先，但所有段落共享同一 speaker、seed 和声学上下文。
- vLLM 原始 PCM 流要求 `speed=1.0`，因此速度设置改为 CustomVoice 的语速指令，不再伪装
  成底层播放倍速。保留整轮单请求、单队列与取消隔离。

## 0.5.42 - 2026-07-28

### 固定声线慢节奏与可听呼吸

- 保持成熟 `0.40`、温柔 `0.35`、轻微魅惑 `0.25` 的选择权重不变，重新生成固定 Base
  角色参考：整体语速降低约一成，并加入自然换气、短促低笑、舒缓呼气和句间停顿。
- Qwen3 Base 不再把“速度”设置当成无效参数；速度低于 `1.0` 时，只在完整语义边界增加
  可听停顿，不做会改变音高的粗暴降采样或播放变速。
- 实时语音正文每轮至少包含一处自然停顿，并根据情境使用一次可直接合成的“嗯……”“呵……”
  “呼……”或“唔……”；亲密场景最多两处，避免机械堆叠或舞台式表演。
- 固定声线继续整轮只合成一次；新权重在两段不同文本上的声纹余弦一致度为 `0.992188`。

## 0.5.41 - 2026-07-28

### 固定角色声线与前台链路减负

- Qwen3 从每段重新采样的 CustomVoice 预置声线切换为 Base 预计算角色权重：
  VoiceDesign 只生成一次参考声，运行时复用同一 ICL 声线；默认偏好为成熟 `0.40`、温柔
  `0.35`、轻微魅惑 `0.25`，普通对话不再被动态 `instructions` 推成激昂演绎。
- Qwen 回复只做一次整段合成，不再按首句、括号或后续句拆成多次声学请求；HTTP PCM 仍保持
  边生成边播放。这样同时消除跨段声线漂移、重复排队和段落接缝。
- 实时语音 Prompt 不再要求隐藏演绎标签；思考、轻笑、叹息、换气和轻哼需要听见时，直接写进
  可说出口的自然口语与停顿，不增加第二次润色模型调用。
- 移除前台协议修复、角色重写、模型 JSON Patch 和档案抽取调用。主模型只负责一次正文；
  人物档案仅接受用户编辑，近期事件摘要、事件推进和角色漂移提示由提交后的后台审计维护。
- 旧版 `serena` 配置在首次加载时只迁移一次到固定 Base 声线；后续用户明确选择仍由配置版本维护。

## 0.5.40 - 2026-07-28

### Qwen3 自然口语演绎

- 移除每段 Qwen3 合成都重复注入的长篇身份、格式和负向导演指令；`neutral` 使用已通过
  原始样本对照的自然聊天短提示，其余演绎仅发送一条短、正向的风格提示，降低播音腔、配音腔和过度激昂。
- 实时语音 Prompt 改为“临时组织出来的说话”：长短口语片段、自然承接和有限犹豫，不再强制
  每句完整收束；普通轮默认 `neutral`，不能仅因亲密或暧昧内容自动升级为强表演标签。
- Qwen 的 HTTP PCM 保持流式，但 CustomVoice 恢复完整上下文 prompt 构造；严格同文对照中，
  该模式与原始直推样本的时长、停顿数量和停顿总量基本一致。
- 早期语块提升到 32–80 字，普通短回复尽量只合成一次；保留“嗯、嗯……”等连接正文的自然犹豫，
  避免一段回答被切成两次录音或被剪成播音稿。新安装默认使用 `serena`，其余声线仍可手动选择。

### 轻量安装器

- 移除 NSIS 内嵌的完整 PowerShell、Git、uv 与 Python 目录。旧安装包会在安装窗口先解压约 900 MB、
  一万多个文件，Windows Defender 扫描时看起来像卡死；新安装包只写 Launcher 与 Core。
- 基础运行时改为首次打开 Launcher 后以可见进度按需部署，支持校验、断点续传、取消与失败说明；安装阶段
  不启动服务、不下载运行时、不占用 ASR/TTS/GPU，也不会修改 `data`。
- Qwen3 的 WSL/vLLM/模型限制保持不变：无论安装还是首次初始化，都不自动安装或下载这些大组件。

## 0.5.39 - 2026-07-28

### 启动、安装与中断恢复加固

- 启动器只在 Core 已启动且用户选中本地引擎时才调度 TTS；云端 TTS 不再无意义拉起本地 Worker。
- GPT-SoVITS 与 Qwen3 的切换改为单一事务：先确认旧的受管引擎在 9 秒内退出，再启动新引擎；
  检测到非当前 Launcher 管理的旧引擎时明确阻止切换，避免误杀进程或双占显存。
- Qwen3 组件增加异步 WSL2、WSL GPU、显存（至少 14 GB）、端口与本地模型启动脚本预检。条件不满足
  时按钮显示原因并禁用；安装包不会自行安装 WSL/vLLM 或下载大模型。
- 取消 LLM、TTS 或 Core 中断后，语音界面清除该意图的音频队列并立即回到“正在监听”；中断文本仍保留，
  不会让旧音频在下一轮继续播放。
- 安装器关闭旧版从固定 600 ms 改为最多 10 秒的受限等待，并提供“重试/取消”说明；安装过程不启动 WSL、
  Qwen、ASR 或 TTS，也不触碰用户数据。

## 0.5.38 - 2026-07-28

### Qwen3-TTS 接入与语音口语化

- 新增 `qwen3-vllm` 本地 TTS Provider：Core 保持既有 PCM 流接口，透传 Qwen3-vLLM 的
  `/v1/audio/speech` 流；默认声线为 `vivian`，仅显示服务端实际提供的声线。
- Qwen3 请求进入与 GPT-SoVITS 共用的单合成队列，支持当前 audio intent 取消和单段重试；
  不预堆多个 vLLM 请求，也不保存 PCM。
- 语音轮可使用隐藏 `[[voice:...]]` 语气标签；标签在流式出口剥离，不显示、不写入会话、
  RAG 或长期记忆，映射为 Qwen3 的 `instructions`，不增加第二次 LLM 调用。
- 桌面启动器新增可选的 Qwen3 WSL2 运行时预检与 8091 健康管理。Qwen 与 ASR 并行启动，
  首次编译/预热只在后台发生；手动切换 GPT-SoVITS/Qwen 时先停止另一引擎，避免双占显存。
- 清理 ASR 就绪依赖和遗留 `voice_emotion_tokens` 传递；Qwen 故障只影响播放，不阻塞聊天或 ASR。

## 0.5.37 - 2026-07-27

### 统一语音会话与并发治理

- 原生 ASR Worker 独占并常驻麦克风：优先 WASAPI 原生采样率、内部转为 16 kHz，并记住
  最后成功端点；不再把 MME 16 kHz 作为唯一通路。
- ASR 事件加入意图、代次与序号；旧会话事件不能写入新轮。原生首帧未到显示“正在准备麦克风”，
  浏览器采集仅作为用户明确点击的备用路径，不再自动抢占设备或刷新页面。
- 本地 TTS 合成改为单请求队列，段落失败只有限重试当前段，不再堆积隐藏 HTTP 合成或清空整个回复。
- 新增只读音频诊断：端点、首帧、PCM 新鲜度、订阅数、恢复原因与 TTS 队列均可检查，不包含音频、
  转写内容或密钥。

## 0.5.36 - 2026-07-27

### 桌面数据目录兜底

- 修复 Core 仅收到 `MINDSPACE_HOME` 时仍回退到 `application/core/runtime`、从而显示新建默认
  档案的问题。现在会稳定映射到该安装根目录下既有的 `data` 目录。
- `MINDSPACE_RUNTIME_DIR` 仍为最高优先级；启动器、恢复流程和手动启动都不会再把用户 JSON、
  会话与配置拆到第二个运行目录。

## 0.5.35 - 2026-07-27

### 全局 R18 素材库与本机只读私有扩展

- 将 R18 Director、风格包和用户授权的本机 DOCX 扩展合并为一个产品级素材库；角色卡
  只保留角色自身的语气、动机与边界覆盖，不能替换全局功能。
- DOCX 以带完整性校验的加密二进制随 Core/EXE 打包，仅在明确开启 R18 时在 Core 内存中
  只读解锁、按阶段抽取少量素材；不会作为独立文件、档案字段、RAG、长期记忆或 API 内容暴露。
- 新增“高强度推进 / 叙事沉浸 / 台词主导”风格选择；R18 首稿不符合质量门时先执行一次
  有界修复，再向文字界面和 TTS 放行。

## 0.5.34 - 2026-07-26

### 原生麦克风设备对象兼容与明确故障提示

- 修复 `sounddevice 0.5.x` 将默认设备返回为 `_InputOutputPair` 时，原生采集器
  将该对象误转为整数、从而在真正打开麦克风前崩溃的问题。
- 输入设备选择改为只接受有输入声道的端点；优先 MME，再尝试默认输入和其他有效端点。
- Windows 暂时没有枚举到输入端点时，页面直接说明原因，不再无限显示“正在连接语音服务”。

## 0.5.33 - 2026-07-26

### 本机麦克风常驻采集与真实音量链路

- 使用原生 PortAudio 输入替代每次进入语音页面都重新打开 Chromium 麦克风。
  ASR Worker 在预热时创建一次 16 kHz 单声道 PCM16 流并在进程生命周期内持有。
- 实机确认 HyperX Cloud III 可以稳定输出持续 PCM；其 MME/WASAPI 端点每次打开
  需要约 10–14 秒，因此打开成本现在与模型预热并行并且只支付一次。
- 没有语音 WebSocket 订阅时只丢弃输入帧，不进行识别、模型调用、消息写入、档案
  修改、RAG 或长期记忆写入；不会增加显存占用。
- 页面进入语音时先检查 Worker 常驻流；可用时只连接 ASR WebSocket，不再申请
  浏览器音轨、创建采集 AudioContext 或加载麦克风 Worklet。
- Worker 以 `asr.level` 返回真实 PCM 振幅，前端波形只按真实输入变化；首帧未到达时
  保持准备状态，不再用跳变动画伪装成正在收音。
- 常驻流停产 5 秒或初次打开超过 25 秒时自动重开设备，订阅队列保持不丢；前端另有
  死流看门狗，快速关闭重开只重连订阅而不拆除后台设备。
- Chromium 单飞采集保留为兼容旧 Worker 和 PortAudio 不可用时的有界备用路径。
- 安装环境新增 `sounddevice 0.5.x`，不增加语音模型、额外大模型调用或常驻 GPU
  占用。

## 0.5.32 - 2026-07-26

### 有界采集恢复去重

- 修复首次采集超时重载后，第二次失败清除恢复标记过早，重复启动回调又将其误判为
  首次失败并继续重载的问题。
- 语音入口改为幂等；同一页面已进入语音时，重复 effect、快捷键或恢复回调不能再次
  创建采集任务。
- 失败次数标记保留到真正取得 live 麦克风音轨后才清除；第二次超时进入明确错误态，
  后续重复回调不能再触发页面重载或设备请求。

## 0.5.31 - 2026-07-26

### 麦克风单飞、物理端点绑定与有界渲染器恢复

- 实机日志确认卡顿时 Core 与 ASR 已在线，但前端在麦克风音轨尚未取得时不断创建
  ASR WebSocket，形成连接风暴；这不是模型加载、VAD、降噪或 TTS 等待。
- 本机采集改为严格单飞：同一时刻只允许一个 `getUserMedia`，成功取得 live 音轨和
  PCM 采集图后才连接常驻 ASR，悬挂请求不能再叠加或拖慢整个页面。
- 首选枚举出的真实物理输入端点并缓存成功的 deviceId，避免 Windows 在 HyperX
  同一 USB 输入/输出同时初始化时反复解析可变的 `default` 别名；不增加 DSP 约束。
- 设备请求 4 秒无响应时只允许一次渲染器级恢复，Core、ASR 和 TTS 继续常驻；再次
  失败会停止自动重试并显示明确恢复入口，不再假装监听或卡住一分钟。
- ASR 传输故障仍可有界重连并复用存活采集图；关闭后快速重开仍复用 15 秒缓存音轨。

## 0.5.30 - 2026-07-26

### Electron 麦克风权限竞态与 USB 端点初始化顺序

- 实机确认 Electron 初始媒体检查可能以 `mediaType=unknown` 到达；旧权限处理器只允许
  `audio`，会让 Chromium 的 `getUserMedia` 长时间等待。通过 DevTools Protocol
  授予同源 `audioCapture` 后，同一渲染器约 7ms 即返回 HyperX live 音轨。
- 本机可信 Core 同源现在允许 `audio`、空值和初始化阶段的 `unknown`，仍拒绝明确
  `video`；媒体请求处理器继续校验请求列表只能包含音频。
- 采集建图顺序改为先取得麦克风 MediaStream，再创建 AudioContext、加载 PCM
  Worklet 和连接静音监视节点；ASR WebSocket 仍与本机采集并行。
- 保留最小 `{audio:true}` 约束、启动代次失效和有界自动恢复。

## 0.5.29 - 2026-07-26

### Windows USB 麦克风首次采集约束修复

- 通过 Electron DevTools Protocol 对实际 HyperX Cloud III 进行对照：麦克风权限
  为 `granted`，`getUserMedia({audio:true})` 能返回 live 音轨，而加入
  `noiseSuppression:false` 后请求超时且音频设备枚举被拖住。
- 浏览器端改用最小采集约束 `{ audio: true }`，删除 deviceId、回声消除、降噪、
  自动增益和声道数的显式协商，交由 Windows/Chromium 选择稳定默认值。
- ASR 低声灵敏度、VAD、回声判定和打断仲裁仍在后端执行，不改变模型、显存或长期
  记忆路径。
- 保留 0.5.28 的启动代次失效、3 秒连接看门狗和 1.5 秒 PCM 首帧自愈，硬件偶发
  故障仍能自动恢复。

## 0.5.28 - 2026-07-26

### 首次采音死锁与采集图自愈

- 修复首次 `getUserMedia`、AudioContext 恢复或 PCM Worklet 加载挂起后，连接看门狗
  虽安排重试、但旧启动单飞标记仍阻止新连接的死锁。
- 超时、采集首帧缺失和持续停产现在会先使旧启动代次失效，再释放旧会话资源并按
  有界退避重建；晚返回的旧采集任务不能覆盖新会话。
- 卡住的采集 AudioContext 不再被下一次启动继承；快速重开缓存图若恢复失败，会
  立即销毁并回退到全新麦克风采集图。
- 连接超时由 12 秒缩短为 3 秒，首次重试延迟缩短为 250ms；采集图建立后 1.5 秒
  仍无 PCM 帧即触发自愈，而不是要求用户手动关闭再打开。
- 新增“首次麦克风 Promise 永不返回”回归测试，验证自动重试确实发起第二次采集。

## 0.5.27 - 2026-07-26

### ASR 常开、采集单飞与 TTS 解耦

- 撤销用户语音提交后持续到 TTS 开始的 ASR 输入锁；LLM 正在等待、失败或尚未产生
  可播放文本时，麦克风 PCM 和识别结果仍会继续进入仲裁，新话语可以取消慢响应。
- 修复过期异步启动清理时把采集 AudioContext 与播放 AudioContext 比较的错误；
  旧会话只能释放自己的节点和音轨，不能关闭新会话正在复用的采集上下文。
- 同一连接代次的语音启动增加单飞保护，已有健康 WebSocket、麦克风音轨和采集上下文
  时不再被重复“恢复”操作拆掉重建。
- ASR WebSocket 断开时保留仍存活的麦克风采集图，并在退避重连时直接复用，避免瞬时
  网络波动触发重复授权、重复 `getUserMedia` 和首句等待。
- “恢复语音”只重建 ASR 传输，不再先创建或等待 TTS 播放上下文；TTS 继续在收到
  AI 可朗读正文后按需工作。
- TTS 等待、暂停或播放失败不再把全局语音状态改成 ASR 连接失败；播放失败时明确提示，
  但识别保持在线。

## 0.5.26 - 2026-07-26

### 实时语音采集管线重写与快速开关防呆

- 修正 0.5.25 的错误验收：WebSocket 被接受不等于客户端已经获得持续 PCM；新增
  Electron 渲染器分阶段追踪，以实际二进制音频帧而非界面文案作为监听成功依据。
- 麦克风采集与 TTS 播放彻底拆成两个 AudioContext，任何播放、停止或 TTS Worklet
  状态变化都不能再暂停、关闭或重建采集图。
- ASR 与麦克风轨道就绪后立即进入监听；首个 PCM 帧改为独立健康检查，不再让界面
  卡在“正在连接语音服务”并反复重建整条链路。
- 退出语音只截断当前模型输出、TTS 队列和本轮 ASR；麦克风轨道立即禁用，并将已经
  建立的采集图保留 15 秒用于快速重开。超时后才停止轨道并挂起上下文。
- 快速“关闭→重新打开”复用同一麦克风轨道和 PCM Worklet，不重复调用
  `getUserMedia`，不重复加载 Worklet，也不重启常驻 ASR/TTS 服务。
- 原始能量候选不再让普通监听界面反复切换“正在聆听”；只有 FSMN-VAD 或识别文本
  确认真声后才改变说话状态。
- Electron 只对 `http://127.0.0.1:8765` 的纯音频请求确定性授权，避免本机可信
  麦克风请求卡在隐式权限检查，同时拒绝其他来源和视频权限。

## 0.5.25 - 2026-07-26

### 实时语音并行首启与采集竞态修复

- 实测已预热 FunASR Worker 直连 `ready` 中位约 `0.9ms`、经 Core 中位约 `1.8ms`；
  首次进入的长等待来自前端采集调度，而不是模型重复加载。
- ASR WebSocket 与麦克风权限、PCM 采集图改为并行启动，不再等完整 WebAudio
  初始化完成后才连接已经常驻的识别服务。
- TTS 播放 Worklet 改为首次播放时按需加载，不再阻塞用户进入监听。
- 修复预采集清理异步关闭共享 AudioContext、而下一次启动仍复用旧引用的竞态；
  共享上下文只允许语音退出路径统一关闭。
- ASR 就绪前缓存最近约 `480ms` PCM，连接建立后按顺序补送，避免用户点进语音页
  立即开口时首字被丢弃。
- 权限拒绝等不可恢复的麦克风错误会先失效当前连接代次，避免已排队的 WebSocket
  关闭事件把明确错误覆盖成无限“正在连接”。

## 0.5.24 - 2026-07-26

### R18 性行为强制推进

- 输入区的 `R18 增强`改为 `R18 性行为`，开关仍由用户显式控制并保存在本机。
- 开启后，本轮核心目标固定为成年人之间明确的性行为与持续推进；单纯亲吻、脱衣、
  抚摸、挑逗、气氛描写、征询或行动前预告不再算作完成模式要求。
- 最终 System 层要求未开始时由角色主动跨过下一阶段，已经开始时直接延续动作、节奏、
  位置、感受或关系变化，不得每轮重置回前戏。
- 最近四条可见角色回复会生成临时推进状态；连续停留在前戏时，本轮 Prompt 明确标记
  已经拖延并要求立即跨阶段。该状态不写入人物卡、会话 RAG 或长期记忆。
- 确定性角色质量检查新增 `r18_missing_sexual_action` 与 `r18_foreplay_loop`，失败回复
  会为下一轮留下精确纠偏，但不额外调用模型、不阻塞流式正文。
- AI 档案新增用户私有 `roleplay.r18_protocol` 接口；用户保存的长篇 R18 协议只在开关
  开启时原文加载到最后 System 层，关闭时不进入 Prompt。该字段不编译为 ASR 热词，
  不进入会话 RAG、结构化长期记忆或公开安装包默认内容。

## 0.5.23 - 2026-07-26

### 语音即时进入、稳定门限与模型连接恢复

- 删除语音入口的重复 ASR/TTS HTTP 预检；点击“开始通话”后立即进入语音页并建立
  WebSocket，面对面模式与场景内容在后台保存，不再吞掉等待期间的第一句话。
- 移除没有可靠采样来源的启动噪声校准、动态噪声底和对应设置项；普通监听门槛调整为
  `-50 dBFS / 120ms`，浏览器关闭可能损伤轻声中文的降噪，只保留回声消除和自动增益。
- 播报中插话使用独立的 `-38 dBFS / 300ms` 门槛，并继续经过 FunASR VAD、回声排除、
  重复抑制和文本仲裁；降低轻声输入门槛不会让环境音直接打断 TTS 或提交给模型。
- 删除实机中会停止回调的 `ScriptProcessor`，并撤销 Chromium 中对音频轨道不产帧的
  `MediaStreamTrackProcessor` 误用；改用标准 `AudioWorklet`，且在“开始通话”的用户
  点击内预热完整采集图。前端只做 PCM 转换，所有噪声判断统一交给 FunASR VAD。
- Windows 短暂切换音频焦点造成的麦克风 `mute` 不再触发整条链路重建；设备真正结束
  或持续十秒无采集帧时才按退避策略恢复。
- OpenAI 兼容客户端保留连接复用，同时使用短保活、TLS/Connect 建连重试并忽略陈旧
  环境代理；只允许在首个 token 到达前重试，避免重复回复和重复模型调用。
- 模型请求失败不再把语音页切到 ASR 错误态；错误作为提示显示，输入门立即解锁，
  用户可以直接继续说话并重新发起下一轮。
- Launcher 不再在主按钮上同步等待最长 90 秒的本地语音模型串行冷加载；Core 先开放，
  ASR 后台加载，本地 TTS 在 ASR 就绪后再启动。启动后还会复查一次健康状态，处理旧
  Launcher 正在退出时留下的短暂端口占用，避免第一次点击假在线、随后全部离线。

## 0.5.22 - 2026-07-26

### R18 成人互动增强

- 输入框下方新增用户显式控制的 `R18 增强`按钮，启用状态保存在本机，并通过
  `adult_mode` 请求字段进入当前轮 Prompt Inspector。
- 关闭增强时，角色卡和上下文原有的成人情境自动识别继续工作，不把普通聊天强行
  转为成人内容。
- 开启增强时，本轮及后续回应只围绕成年人之间的亲密、欲望、身体互动和相关关系
  张力推进，不主动漂移到催睡、健康管理、家务或普通闲聊。
- 增强层要求直接、具体、连续地推进角色自己的动作、感受和欲望，避免含糊暗示、
  反复选择题和客服式照顾，同时不替用户虚构动作、反应或感受。
- 文字聊天继续使用“全角括号内写动作、括号外写台词”；实时语音继续输出角色可
  直接说出口的纯口语，不混入舞台旁白。

## 0.5.21 - 2026-07-26

### ASR 首启采音、自愈监测与纯口语输出

- 修复自适应噪声校准使用偏高分位的问题：校准窗口内不再关闭人声门，并改用
  低分位样本估算底噪，用户进入语音页后可以立即开口。
- 移除 Launcher 日常启动前重复执行的 CUDA/Torch/FunASR 全量导入预检；依赖完整性
  由安装阶段原子标记保证，运行状态由 Worker 健康检查和有界恢复负责，不再因一次
  冷启动抖动删除就绪标记并要求第二次“继续修复”。
- 语音页只有在 ASR 就绪并收到真实 PCM 音频帧后才显示“我在听”；单纯建立
  WebSocket 不再形成假就绪。
- “开始通话/面对面”点击事件内立即预热麦克风和采集 AudioContext，避免等待设置
  请求后丢失浏览器用户激活；ASR 改用 Chromium 内稳定的约 43ms PCM 回调与
  极低增益调度支路，绕过首次 AudioWorklet 图偶发不被拉取的问题。
- 增加麦克风采音看门狗和轨道生命周期处理。设备断开、音轨持续静音、
  AudioContext 暂停或采集回调停产时释放旧资源并按退避策略自动重连。
- 暂时性的设备占用与 Chromium 音频服务重启自动恢复；权限拒绝与平台不支持保留
  明确错误和手动恢复入口。
- 实时语音 Prompt 禁止圆括号、动作旁白和神态标签，要求把动作、外观与体感写成
  角色亲口说出的第一人称口语；流式正文、最终回复和持久化前均有确定性括号移除兜底。
- 屏幕文字聊天明确恢复“台词在括号外、动作与神态描写在全角括号内”的格式边界；
  人物卡示例只控制语气，不能再把动作叙述混成角色对白。

## 0.5.20 - 2026-07-26

### TTS 纯标点隔离、队列续播与桌面版本固定

- 流式 TTS 分段增加可朗读文字判断；独立的省略号、破折号和装饰符号不会再形成
  GPT-SoVITS 请求，流式到达的前导省略号会保留并附着到下一段正文。
- Core 在构造 `StreamingResponse` 前拒绝纯标点输入，Worker 在发送 200 响应头前
  进行同类校验，避免“请求成功但音频为空”的假成功。
- 前端把无正文、无效文本归类为可跳过的单段错误，不再清空同一回复中已经排队的
  后续有效正文；网络、服务和播放器故障仍按整轮失败处理。
- 桌面入口固定使用独立 Launcher 目录，运行环境和私有资料继续位于独立的
  `MINDSPACE_HOME`，源码、启动器和用户数据不互相覆盖。

## 0.5.19 - 2026-07-26

### 角色卡 V2、检索分级与角色正文减负

- AI 人物档案新增用户可直接维护的 `roleplay` 区域，拆分角色自我、自主性、
  语言风格、常态场景、历史后校准和五类对话示例；旧档案加载时只补空字段，
  不覆盖用户已有内容。
- 运行状态新增 `roleplay_state` 场景与角色驱动力接口，为转场、角色自身活动和
  未完互动保留结构位置；这些字段默认不由角色正文自行写回。
- 会话 RAG 只接收用户原话和结构化记忆；原始助手回复、主动续话正文和隐藏触发
  继续保存在完整会话与最近 8 轮历史中，但退出长期语义召回。
- “嗯、好”等确认词、单独转场和主动续话不触发原始聊天向量检索；结构化长期记忆
  仍可按现有规则参与召回。
- 普通正文轮的 `json_update` 固定为空，用户明确提供可记忆事实时再由独立、
  限时、字段白名单提取器处理；删除校正和首次档案初始化保留兼容例外。
- 主动续话在延续动作、表达看法、具体观察和个人新话题之间轮换；确定性质量层标记
  重复、无关照料循环、通用助手口吻和连续外包决定，并在下一轮给出短纠偏。
- 人物档案表单补齐所有 V2 中文字段名；分类示例按当前场景最多加载两条，
  避免整份示例和无关风格常驻 Prompt。

## 0.5.18 - 2026-07-26

### ASR/TTS 持续恢复、单通道调度与分段重构

- Core 在本地 TTS Worker 之前增加可取消的单通道调度锁；同一回复的多个语音段
  不再同时进入 GPT-SoVITS，取消的等待请求不会遗留为后台旧任务。
- 同一 `request_id` 可登记多个 TTS 任务，打断时统一取消，不再由最后一个分段
  覆盖前面仍在运行的任务。
- 前端取消“每到一个句子就立即并行预取”；只在当前合成流完成后准备一个后续段，
  避免单模型锁后形成无法观察的请求积压。
- ASR 在取得麦克风前检查 Worker 就绪状态；模型冷启动或意外断线时以最高 5 秒
  间隔持续恢复，用户退出语音后才停止，不再十几秒后永久放弃。
- TTS 冷启动最多等待 90 秒并显示启动状态；模型尚未监听端口不会直接把当前语音轮
  判为崩溃。
- 实时语音 TTS 改为“首句优先、括号独立、正文整段累计”：第一句尽快开口，
  `（……）` 内容作为独立语音段，其余正文跨句跨段合并，仅在下一个括号或轮末截断。
- Launcher 的 ASR CUDA 依赖校验改为异步子进程；冷启动期间窗口仍可刷新和停止，
  不再因同步导入 Torch 而进入“未响应”状态。

## 0.5.17 - 2026-07-26

### 语音服务崩溃恢复与页面解锁

- Launcher 对自己启动的 Core、ASR、TTS 子进程增加有界崩溃恢复：
  依次等待 1 秒、2.5 秒和 5 秒重启，第四次连续失败停止重试并写入诊断日志。
- 前端 ASR WebSocket 意外断开后释放旧麦克风、AudioContext、Worklet 和连接，
  最多自动重连 4 次；用户退出与手动停止不会触发后台复活。
- TTS 增加流数据空闲和播放结束看门狗；失败时取消预取、清空待播队列并解除
  ASR 输入门，避免全屏语音页长期停留在不可操作状态。
- 麦克风和播放音量的 React 更新限频到最高 20 FPS，降低持续语音时主线程压力。
- 修复 ASR 代理断开后的未回收 Task 异常，并将 WebSocket disconnect 视为正常终态。
- GPT-SoVITS 英文分词在缺少 NLTK 词性语料时使用确定性降级；流式请求被客户端
  取消后不再尝试向已断开的连接写第二个 JSON 错误。

## 0.5.16 - 2026-07-25

### 恢复 8 轮窗口之外的 RAG 选择性召回

- 修复 Prompt 组装使用完整持久历史对 RAG 聊天片段去重的问题；旧轮命中不再因
  数据库中存在同一原文而被误删。
- 去重范围改为模型本轮直接可见的最近 8 个 round，避免同一段近期原文同时出现在
  历史层和召回层。
- 完整聊天继续保存在数据库与检索索引中；最近 8 轮负责连续性，更早内容只在
  语义相关时由 RAG 召回，且维持低可信候选身份。
- 增加跨 8 轮边界测试，同时验证“旧轮可召回”和“近期不重复”。

## 0.5.15 - 2026-07-25

### 模型历史固定为最近 8 轮原始对话

- Context Ledger 继续保存完整会话用于页面、恢复和审计，但主模型历史层无条件只读取
  最近 8 个 round，不再等待上下文达到 65% 才缩减原文。
- 历史层只允许可见用户原话和最终助手正文；角色审计纠偏、权威 JSON Patch、
  删除校正和隐藏的主动续话触发均不再进入后续模型历史。
- 主动续话产生的助手可见正文仍可承接，但服务端伪造的隐藏 user 占位不持久化。
- 最新 44 轮实测历史由约 10,439 字符降到 1,819 字符，仅保留第 37–44 轮。

## 0.5.14 - 2026-07-25

### 移除 AI 本机观测工具与进一步压缩 Prompt

- 从 AI 能力注册、确定性路由、规划授权和执行器中删除本机硬件状态与
  Mindspace 服务健康状态工具；Launcher 和诊断 API 继续独立负责运行检查。
- 设置页移除“读取本机状态”和“检查 Mindspace 服务”，旧配置字段在加载时自动忽略。
- 普通零调用轮不再注入工具目录、能力设置或 `call_count=0` Prompt；联网真实性继续由
  生成后的服务端确定性校验保证。
- 实际查询轮只向正文模型提供精简执行状态与查询结果，不再发送完整工具注册表和内部设置。
- AI 可调用能力由 6 个减至 4 个：本地知识、打开网页、网页搜索和热点检索。

## 0.5.13 - 2026-07-25

### 角色演绎尾部校准与 Prompt 减负

- 借鉴 SillyTavern 的 Post-History Instructions 结构，在当前输入之后追加
  临时 System 角色演绎校准；权威角色卡仍位于稳定前缀，不破坏缓存布局。
- 明确“角色自主”不等于故意反对用户：角色自身意愿与当前邀请一致时直接推进，
  不再用空泛保证、反复询问和突然转回中性闲聊证明独立。
- 当前场景、近期原始对话和运行状态已经满足角色卡中的成人/亲密情境条件时，
  自动激活对应 contextual rules；明确继续信号不再要求重复开关词。
- 当前 Epoch 已经包含的原始聊天不会再次作为 RAG 片段进入同一 Prompt，避免
  旧回复被重复强化并减少无意义 token。
- 未执行能力的普通对话只注入精简的 `call_count=0` 真实性状态，不再附带整份
  工具目录；真实查询轮仍保留完整能力、计划、结果和来源约束。
- 增加 Prompt 顺序、近期聊天去重、亲密情境激活和临时层不可持久化测试。

## 0.5.12 - 2026-07-25

### 语音括号内容完整朗读

- 实时语音不再丢弃全角或半角括号中的动作、神态、触感和语气内容；
  括号符号转换为自然停顿，内部文字全部进入 TTS。
- 流式回复遇到尚未闭合的括号时等待后续 token，闭合或本轮结束后再完整分句，
  避免只读半句或漏掉后半段动作。
- 普通文字页的自动与手动朗读继续过滤括号舞台说明，不扩大本次行为变化范围。
- 语音 Prompt 和设置页同步声明括号内容会被朗读，避免模型继续把重要互动写进
  一个客户端会丢弃的通道。

## 0.5.11 - 2026-07-25

### 角色自主性与沉浸表达

- AI 人物 JSON 从普通数据层提升到首条 System 中的权威角色卡，身份、性格、
  价值和关系立场不再被当前用户指令轻易覆盖。
- 增加角色自我一致性一级原则：角色可以赞同、拒绝、反驳、调侃或改变话题，
  不把机械顺从、道歉和即时取悦用户作为最高目标。
- 用户纠正只覆盖用户自身事实、偏好和当下事件；长期改写 AI 角色必须由用户
  在人物档案编辑器中保存新版本。
- 移除“现实接触只能写成愿望或假设”的应用级限制；角色可以在共同沉浸场景中
  直接描述自己的穿着、动作、距离、触感和发起的互动，不替用户决定反应。
- AI 角色卡不再同时复制进权威 JSON 数据层，提升优先级的同时避免重复 Prompt
  token；联网真实性、可信数据和 JSON 写入校验继续由服务端保证。

## 0.5.10 - 2026-07-25

### 语音会话可靠性与自然插话

- 进入语音时先验证 ASR、TTS 和浏览器音频上下文，服务未就绪不再进入
  “看起来正在通话但没有声音”的假就绪状态。
- 播放上下文贯穿整次语音会话复用；普通打断只停止当前播放节点，退出语音
  时才关闭上下文，降低二次播放无声概率。
- TTS 增加响应、首个 PCM、空音频和播放器启动看门狗，失败时解除输入锁并
  提供“恢复语音”，不再无限停留在无声状态。
- 语音入口保存请求支持超时和主动取消；等待期间取消、右上角、Esc 和点击
  遮罩都能退出，避免透明弹窗或悬挂请求锁住整个页面。
- 旧 `350 ms` 续话窗口迁移为 `1100 ms`；短句、犹豫尾词和插话后表达使用
  更长的自适应等待，ASR 自动标点不再触发 `160 ms` 抢先发送。
- VAD、持续时间和回声排除均成立时，两三个字的中文短插话可以有效打断；
  咳嗽、回声和整段低置信内容仍不能提交 LLM 或写入长期记忆。

## 0.5.9 - 2026-07-24

### 用户角色卡与第一认同性别

- 支持将本地人物自述压缩为结构化用户档案，通过现有档案 API 写入，
  保留历史版本、revision、审计记录和记忆索引；私人档案不进入源码或安装包。
- 用户档案与 AI 档案的 `identity.gender` 只接受“男”或“女”，人物档案
  表单提供独立的手动选择器。
- 性别属于用户所有的只读模型身份字段；模型 JSON Patch、档案初始化和记忆
  抽取不能自行修改。
- 主模型第一条 System 内容先声明用户与 AI 的第一认同性别，再进入角色设定、
  权威 JSON、历史、检索和工具上下文。
- 性别用于称谓、关系角色、身体归属和自我指代，但不替代人物档案中的性格、
  关系与行为规则。

## 0.5.8 - 2026-07-24

### 通话与面对面互动

- 点击实时语音时先选择“通话”或“面对面”；默认仍为原有通话逻辑。
- 面对面模式提供可选场景输入框，并持久保存上次模式和场景内容。
- 每轮语音请求携带受校验的 `voice_context`，面对面场景以临时高优先级
  System 层进入模型输入。
- 角色可自然描述自身外观、动作、距离和体感线索，但不能替用户断言动作、
  反应或感受。
- 场景属于用户控制的互动配置，不进入长期对话历史、人物事实、向量记忆或
  JSON Patch 证据。

## 0.5.7 - 2026-07-23

### 可信输入、运行恢复与调用预算

- 模型调用按规划、研究复核、正文、协议修复和记忆抽取独立限额，单轮总上限为五次。
- 普通闲聊不会因“今天”等时间词误触联网；已有可用正文时由服务端确定性补齐安全协议。
- 流式正文按检查点持久化，刷新或断线只恢复订阅，Core 重启保留已生成部分且不自动重跑。
- 检索、工具和调度信息降为审计可见，ASR 低置信候选只在当前轮临时参考。
- 用户可从人物卡直接编辑档案、检查 revision 冲突并恢复历史版本。
- 新增默认脱敏的模型输入检查器。
- 中文 ASR 增加回声排除、打断限频、重复抑制和低置信草稿仲裁。
- 外部能力严格串行执行，HTTP 客户端保持连接池复用。
- Launcher 在创建窗口前初始化更新与运行时管理器，消除首屏状态查询竞态。

## 0.5.6 - 2026-07-23

### 档案写回与时间、联网判断修复

- 人物与状态档案新增表单编辑、保存状态和明确错误反馈，同时保留高级 JSON 模式。
- 有记忆价值的语句才触发结构化抽取，AI 自述仅凭本轮逐字证据写入 agent 字段。
- 明确联网请求在私有规划失败时保留确定性调用，并拦截未执行却声称已搜索的表述。
- 服务端直接给出日期、星期、周末与明日判断，避免模型自行换算出错。
- 简短确认不再自动等同于结束话题、解决情绪或准备休息。

## 0.5.5 - 2026-07-23

### 中文整句复核与 CUDA 调度

- 保留 Paraformer 实时字幕，新增 Fun-ASR Nano 中文整句复核与无模型安全回退。
- 含糊尾词、句末和播放场景使用动态断句，旧默认 250 ms 迁移为 600 ms。
- CUDA 推理单通道且流式优先，Nano 固定单批次、中文和有限热词以压低占用。
- Launcher 等待 ASR 加载完成后再启动本地 TTS，避免大模型并行搬运和显存争抢。
- 最终事件保留流式初稿、复核原稿、纠偏结果、端点原因和明确回退状态。

## 0.5.4 - 2026-07-23

### 低延迟与断线恢复

- 本机状态改为能力触发时采集，普通对话不再无条件扫描本机。
- 向量查询单飞并复用既有向量，避免同轮分支和二次工具调用重复编码。
- HTTP 客户端统一连接池复用。
- 流式事件支持序号、重连续传与重复过滤，前端按动画帧批量渲染。
- 启动器合并并发启动、并行健康探测并防止停止期间误拉起。
- 情绪模型和运行链路移除，仅保留明确的零开销接口边界。

## 0.5.3 - 2026-07-22

### 角色优先与事实约束

- 角色设定提升为首条 System，回复首先从人物关系、性格和当下感受出发。
- AI 只表示存在媒介，不再触发通用助手、客服或问答口吻。
- 召回内容改为候选线索，未获当前输入、权威 JSON 或近期原始对话确认时不得引用为个人事实。
- 空字段明确表示未知，不再由常识、相似语义或知识库补成人物偏好。
- JSON 输出协议压缩并后置，与用户可见的角色回复分离。

## 0.5.2 - 2026-07-22

### 实时识别与智能词表

- 新增自适应环境噪声基线，呼吸和瞬时噪声不再直接打断朗读。
- 语音片段先合并再发送，生成期间的补充话会与原输入组成同一轮重发。
- 播放中未达到打断条件的有效语音延后保留，不再丢失或拆成多轮。
- 新增可在线编辑的识别词表，人物名称和专用名词高强度增强。
- 人物与状态 JSON 自动投影为识别热词，不进入 Prompt，也不增加 LLM 判断负担。
- 识别纠偏元数据独立审计并限制容量增长。

## 0.5.1 - 2026-07-21

### 时间感知与自然续接

- 文字与语音统一记录服务端 UTC 时间、当地时区和对话间隔。
- 新增可配置的沉默后自然续接，文字和语音可分别设置等待时间。
- 主动续接不伪造用户输入、不催促用户回应，也不会触发 JSON 写入。
- 语音插话记录实际播放位置，向下一轮区分已听和未听内容。
- 播放中先降低音量，VAD 或 ASR 确认人声后才真正打断。

## 0.5.0 - 2026-07-21

### 启动器分类与可靠下载

- 首页改为状态概览与基础环境、语音能力、下载存储、更新维护四个分类。
- 基础环境按真实依赖顺序安装，自动复用已就绪组件和断点缓存。
- 下载失败显示错误码、阶段、操作编号，并可导出脱敏诊断报告。
- 更新应用时保留私有环境、模型、数据和下载缓存。
- 增加 GPT-SoVITS V4 角色音色启动、停顿和韵律修复。

## 0.4.7 - 2026-07-20

### 版本公告与更新中心

- 新增启动时新版本公告，仅在检测到高于当前版本时弹出。
- 公告弹窗接入签名下载、校验、安装和回滚流程。
- 新增历史版本公告记录。

## 0.4.6 - 2026-07-20

### 语音与存储修复

- 修复残留 ASR CUDA 环境被误判为整套应用损坏。
- 实时聆听支持续装 CUDA 依赖与 ASR、VAD、标点模型。
- 支持迁移环境、模型、数据与 Core 到其他磁盘。
- 新安装包允许选择安装目录。

## 0.4.5 - 2026-07-20

### 更新与本地语音稳定性

- 修复 TTS 本地链路选择回跳。
- 移除首句独立语气词的朗读。
- 恢复 ASR 与 CosyVoice 共享运行时。
- 普通更新优先使用 Core 原子替换。

## 0.4.4 - 2026-07-20

### 零环境安装修复

- 修复更新时错误清理私有环境的问题。
- 核心服务优先启动，本地语音保持硬件可选。
- 组件下载支持续传、校验和失败恢复。
